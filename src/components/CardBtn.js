import React from 'react';

function CardBtn(props) {
    return (
        <button 
        type="button" 
        className="btn btn-info" 
        onClick={searchedUser}>Submit</button>
    );
    
}

export default CardBtn;